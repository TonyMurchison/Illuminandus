<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tonymurchison.illuminandus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LevelSelect" />
        <activity android:name=".LevelSelectV2" />
        <activity android:name=".LevelPlay" />
        <activity android:name=".LevelPlay2" />
        <activity android:name=".PauseScreen"/>
        <activity android:name=".FinishedScreen"/>
        <activity android:name=".InfoScreenLevel"/>
        <activity android:name=".InformationScreen"/>

        <!--
        <activity
            android:name=".InfoScreenLevel"
            android:label="InfoScreenLevel"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".InformationScreen"
            android:label="@string/title_activity_information_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>

            -->
        <uses-permission android:name="android.permission.WAKE_LOCK"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
